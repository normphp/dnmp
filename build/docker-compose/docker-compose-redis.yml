version: '3.3'
services:
  redis:
    env_file:
      - .env
    build: ${REDIS_IMAGE}
    networks:
      - ${REDIS_NETWORKS}
    ports:
      - "6379:${REDIS_PORTS}"
    volumes:
      - /docker/normphp/dnmp/data/redis:/data
    restart: always
networks:
  dnmpNat:
